---
i18nReady: true
title: Dropdown
---

import { Tabs, TabItem } from '@astrojs/starlight/components';
import PreviewCard from '~/components/PreviewCard.astro';
import DropdownScript from '~/components/DropdownScript.astro';
import { Dropdown, Button, Row } from 'studiocms:ui/components';

:::caution[Helper required]
This component requires a **helper**. Make sure to read the documentation carefully so you know how to use it.
:::

A dropdown component that takes care of all the annoying bits for you, with added support for programmatic triggers on top.

## Usage

<Tabs>
  <TabItem label="Preview">
    <PreviewCard>
      <Dropdown id='usage-dropdown' options={[{ label: 'Option 1', value: 'opt-1'},{ label: 'Option 2', value: 'opt-2'}]}>
        <Button color="primary">
          Trigger Dropdown
        </Button>
      </Dropdown>
    </PreviewCard>
  </TabItem>
  <TabItem label="Code">
    ```astro showLinenumbers title="DropdownExample.astro"
    ---
    import { Button } from 'studiocms:ui/components/button';
    import { Dropdown } from 'studiocms:ui/components/dropdown';
    ---

    <Dropdown 
      id='dropdown' 
      options={[
        { label: 'Option 1', value: 'opt-1'},
        { label: 'Option 2', value: 'opt-2'},
      ]}
    >
      <Button color='primary'>
        Trigger Dropdown
      </Button>
    </Dropdown>
    ```

    ```ts twoslash showLinenumbers title="Script Tag"
    /// <reference types="@studiocms/ui/v/types" />
    // ---cut---
    import { DropdownHelper } from 'studiocms:ui/components/dropdown';

    const dropdown = new DropdownHelper('dropdown');
    ```
  </TabItem>
</Tabs>

### Dropdown Position

You can change the dropdowns position by supplying the `align` prop.

<Tabs>
  <TabItem label="Preview">
    <PreviewCard>
      <Row>
        <Dropdown id='align-start-dropdown' align='start' options={[{ label: 'Option 1', value: 'opt-1'},{ label: 'Option 2', value: 'opt-2'}]}>
          <Button color="primary">
            Start-aligned
          </Button>
        </Dropdown>
        <Dropdown id='align-center-dropdown' options={[{ label: 'Option 1', value: 'opt-1'},{ label: 'Option 2', value: 'opt-2'}]}>
          <Button color="primary">
            Center-aligned
          </Button>
        </Dropdown>
        <Dropdown id='align-end-dropdown' align='end' options={[{ label: 'Option 1', value: 'opt-1'},{ label: 'Option 2', value: 'opt-2'}]}>
          <Button color="primary">
            End-aligned
          </Button>
        </Dropdown>
      </Row>
    </PreviewCard>
  </TabItem>
  <TabItem label="Code">
    ```astro "align='start'" showLinenumbers title="DropdownAlignStartExample.astro"
    ---
    import { Button } from 'studiocms:ui/components/button';
    import { Dropdown } from 'studiocms:ui/components/dropdown';
    ---

    <Dropdown 
      id='dropdown'
      options={[
        { label: 'Option 1', value: 'opt-1'},
        { label: 'Option 2', value: 'opt-2'},
      ]}
      align='start'
    >
      <Button color='primary'>
        Start-aligned
      </Button>
    </Dropdown>
    
    <!-- Other buttons omitted for simplicity -->
    ```
    ```ts twoslash showLinenumbers title="Script Tag"
    /// <reference types="@studiocms/ui/v/types" />
    // ---cut---
    import { DropdownHelper } from 'studiocms:ui/components/dropdown';

    const dropdown = new DropdownHelper('dropdown');
    ```
  </TabItem>
</Tabs>

### Right-Click Triggering

You can tell the dropdown wrapper to only show the dropdown when it's children are right-clicked:

<Tabs>
  <TabItem label="Preview">
    <PreviewCard>
      <Dropdown id='right-click-dropdown' triggerOn='right' options={[{ label: 'Option 1', value: 'opt-1'},{ label: 'Option 2', value: 'opt-2'}]}>
        <Button color="primary">
          Right Click
        </Button>
      </Dropdown>
    </PreviewCard>
  </TabItem>
  <TabItem label="Code">
    ```astro "triggerOn='right'" showLinenumbers title="DropdownRightClickExample.astro"
    ---
    import { Button } from 'studiocms:ui/components/button';
    import { Dropdown } from 'studiocms:ui/components/dropdown';
    ---

    <Dropdown
      id='dropdown'
      options={[
        { label: 'Option 1', value: 'opt-1'},
        { label: 'Option 2', value: 'opt-2'},
      ]}
      triggerOn='right'
    >
      <Button color='primary'>
        Right Click
      </Button>
    </Dropdown>
    ```
    ```ts twoslash showLinenumbers title="Script Tag"
    /// <reference types="@studiocms/ui/v/types" />
    // ---cut---
    import { DropdownHelper } from 'studiocms:ui/components/dropdown';

    const dropdown = new DropdownHelper('dropdown');
    ```
  </TabItem>
</Tabs>

:::note
You can also tell the dropdown to trigger on both left- & right-click by using the `triggerOn='both'` prop.
:::

### Disabled

You can disable a dropdown by passing the `disabled` prop.

```astro "disabled" showLinenumbers title="DropdownDisabledExample.astro"
<Dropdown disabled {/* ... */}>
  <!-- ... -->
</Dropdown>
```

### Option Options

No, you read that right. You can customize the options too. You can individually disable them, turn them into links and also change their color to any of the default colors:

<Tabs>
  <TabItem label="Preview">
    <PreviewCard>
      <Dropdown 
        id='options-dropdown' 
        options={[
          { label: 'Default', value: 'default', color: 'default' },
          { label: 'Primary', value: 'primary', color: 'primary' },
          { label: 'Success', value: 'success', color: 'success' },
          { label: 'Warning', value: 'warning', color: 'warning' },
          { label: 'Danger', value: 'danger', color: 'danger' },
          { label: 'Info', value: 'info', color: 'info' },
          { label: 'Monochrome', value: 'mono', color: 'mono' },
          { label: 'Disabled', value: 'disabled', disabled: true },
          { label: 'As Link', value: 'custom-href', href: '/' },
        ]}
      >
        <Button color='primary'>
          Customized Options
        </Button>
      </Dropdown>
    </PreviewCard>
  </TabItem>
  <TabItem label="Code">
    ```astro ins={7-14} showLinenumbers title="DropdownOptionsExample.astro"
    ---
    import { Button } from 'studiocms:ui/components/button';
    import { Dropdown } from 'studiocms:ui/components/dropdown';
    ---

    <Dropdown 
      id='dropdown' 
      options={[
        { label: 'Default', value: 'default', color: 'default' },
        { label: 'Primary', value: 'primary', color: 'primary' },
        { label: 'Success', value: 'success', color: 'success' },
        { label: 'Warning', value: 'warning', color: 'warning' },
        { label: 'Danger', value: 'danger', color: 'danger' },
        { label: 'Info', value: 'info', color: 'info' },
        { label: 'Monochrome', value: 'mono', color: 'mono' },
        { label: 'Disabled', value: 'disabled', disabled: true },
        { label: 'As Link', value: 'custom-href', href: '/' },
      ]}
    >
      <Button color='primary'>
        Customized Options
      </Button>
    </Dropdown>
    ```
    ```ts twoslash showLinenumbers title="Script Tag"
    /// <reference types="@studiocms/ui/v/types" />
    // ---cut---
    import { DropdownHelper } from 'studiocms:ui/components/dropdown';

    const dropdown = new DropdownHelper('dropdown');
    ```
  </TabItem>
</Tabs>

#### Icons

The dropdown items also support icons. You can pass an icon name to the option object:

<Tabs>
  <TabItem label="Preview">
    <PreviewCard>
      <Dropdown 
        id='icons-dropdown' 
        options={[
          { label: 'Notifications', value: 'notifications', icon: 'heroicons:bell' },
          { label: 'Settings', value: 'settings', icon: 'heroicons:cog-6-tooth' },
          { label: 'Profile', value: 'profile', icon: 'heroicons:user' },
        ]}
      >
        <Button color='primary'>
          Customized Icons
        </Button>
      </Dropdown>
    </PreviewCard>
  </TabItem>
  <TabItem label="Code">
    ```astro ins={7-14} showLinenumbers title="DropdownIconsExample.astro"
    ---
    import { Button } from 'studiocms:ui/components/button';
    import { Dropdown } from 'studiocms:ui/components/dropdown';
    ---

    <Dropdown 
      id='dropdown' 
      options={[
        { label: 'Notifications', value: 'notifications', icon: 'heroicons:bell' },
        { label: 'Settings', value: 'settings', icon: 'heroicons:cog-6-tooth' },
        { label: 'Profile', value: 'profile', icon: 'heroicons:user' },
      ]}
    >
      <Button color='primary'>
        Customized Icons
      </Button>
    </Dropdown>
    ```
    ```ts twoslash showLinenumbers title="Script Tag"
    /// <reference types="@studiocms/ui/v/types" />
    // ---cut---
    import { DropdownHelper } from 'studiocms:ui/components/dropdown';

    const dropdown = new DropdownHelper('dropdown');
    ```
  </TabItem>
</Tabs>

### Controlling Dropdowns Programmatically

You know how, this entire page, we've been assigning our `DropdownHelper` to a variable? There's a reason for that. The DropdownHelper allows
you to access some built-in functions for programmatically toggling, showing and hiding the dropdown. You can even register a callback for when one of the 
dropdowns options has been clicked!

```ts twoslash showLinenumbers title="Script Tag"
/// <reference types="@studiocms/ui/v/types" />
// ---cut---
import { DropdownHelper } from 'studiocms:ui/components/dropdown';

const dropdown = new DropdownHelper('dropdown');

dropdown.registerClickCallback((value) => {
  // `value` will be the value you gave your option in the dropdown wrapper.
  console.log(value);
});

// You can toggle the dropdown like this:
dropdown.toggle();

// Instead of toggling, you can simply show or hide the dropdown:
dropdown.show();
dropdown.hide();
```

{/* All the helpers are in here 🥲 */}
<DropdownScript />
